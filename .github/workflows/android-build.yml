name: Build Android APK

# Se puede lanzar manualmente desde la pestaña "Actions" en GitHub
# (botón "Run workflow"), y opcionalmente cada vez que se sube código
# a main. Quita el bloque "push" si solo quieres lanzarlo a mano.
on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      - name: Descargar el código
        uses: actions/checkout@v4

      - name: Instalar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "lts/*"
          cache: "npm"

      - name: Instalar Java (JDK 17)
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"

      - name: Clear Android SDK Cache
        run: |
          rm -rf ~/Android/Sdk/ndk
          rm -rf ~/.android/cache

      - name: Cachear dependencias de Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('android/**/*.gradle*', 'android/gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: |
            gradle-${{ runner.os }}-

      - name: Instalar dependencias del proyecto
        run: npm ci

      - name: Generar proyecto nativo (expo prebuild)
        run: npx expo prebuild --platform android

      - name: Usar un keystore de debug fijo (evita conflicto de firma al reinstalar)
        run: |
          mkdir -p ~/.android
          cp debug.keystore ~/.android/debug.keystore

      - name: Dar permisos de ejecución a gradlew
        run: chmod +x android/gradlew

      - name: Compilar APK de release (JS empaquetado, no necesita Metro)
        working-directory: android
        run: ./gradlew assembleRelease -PreactNativeArchitectures=arm64-v8a --no-daemon

      - name: Subir el APK como artifact descargable
        uses: actions/upload-artifact@v4
        with:
          name: puntos-por-voz-release-apk
          path: android/app/build/outputs/apk/release/app-release.apk
          retention-days: 14
